# Sui chain (sui:mainnet, sui:testnet, sui:devnet)
VITE_SUI_CHAIN=sui:testnet

# Package ID from sui client publish (replace after deployment)
VITE_SUI_STREAM_PACKAGE_ID=0x<your_package_id>

# Move module name (matches streaming_payment::stream)
VITE_SUI_STREAM_MODULE=stream

# Sui Clock object (0x6 on mainnet/testnet/devnet)
VITE_SUI_CLOCK_OBJECT_ID=0x6
